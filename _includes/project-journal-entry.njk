{% extends "base.njk" %}
{% block content %}
{% set project = collections.projectsBySlug[projectSlug] %}
<div class="breadcrumb">
  <div class="wrap"><div class="inner"><a href="/projects/">Projects</a> / <a href="{{ project.url }}">{{ project.data.title }}</a> / {{ title }}</div></div>
</div>
<main class="wrap">
  <div class="art-body">
    <p class="accession accession-project">{{ project.data.title }}</p>
    <p class="accession">Journal entry No. {{ no }}</p>
    <h1>{{ title }}</h1>
    {% if project.data.tags %}
    <div class="entry-tags">{% for t in project.data.tags %}<a href="#">{{ t }}</a>{% endfor %}</div>
    {% endif %}
    {{ content | safe }}
    <p><a href="{{ project.url }}">&larr; Back to {{ project.data.title }}</a></p>
  </div>
</main>
{% endblock %}
